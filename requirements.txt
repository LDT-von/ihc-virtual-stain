# Core
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# Flow Matching & diffusion
# (FM 在 torch + 自定义实现即可，无需额外依赖)
# 可选：diffusers>=0.25  # 借鉴其 UNet 实现

# 图像处理
Pillow>=10.0
opencv-python-headless>=4.8
albumentations>=1.3

# 医学影像
# monai>=1.3  # 病理图像专用增强/IO

# 评测指标
scikit-image>=0.21     # SSIM / PSNR
scikit-learn>=1.3

# 训练辅助
tqdm>=4.66
tensorboard>=2.15
einops>=0.7

# 工具
matplotlib>=3.7
timm>=0.9              # 备用骨干网络
hydra-core>=1.3        # 可选：配置管理